# skywatch API — Python requirements
# Install: pip install -r requirements.txt
# For development: pip install -r requirements.txt ruff black pytest pytest-asyncio

# ── Core web framework ──────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── HTTP client (async) ─────────────────────────────────────────────────────
httpx>=0.27.0

# ── Data validation ─────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Image processing (CV heuristic classifier) ──────────────────────────────
Pillow>=10.3.0
numpy>=1.26.0
scipy>=1.13.0

# ── Optional: PyTorch for EfficientNet / CLIP classifier ────────────────────
# Uncomment to enable neural-network-based classification
# torch>=2.3.0
# torchvision>=0.18.0
# open_clip_torch>=2.24.0
